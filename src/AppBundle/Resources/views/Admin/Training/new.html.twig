{% extends 'AppBundle:Admin:layout.html.twig' %}

{% block body %}
    <h1>Création d'entrainement</h1>

    {{ form_start(form) }}
    {{ form_row(form.hour, { 'label': 'Heure' }) }}
    {{ form_row(form.day, { 'label': 'Jour' }) }}
    {{ form_row(form.team, { 'label': 'Equipe' }) }}
    {{ form_row(form.place, { 'label': 'Salle' }) }}
    {{ form_rest(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-success">ajouter</button>
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('admin_training_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
