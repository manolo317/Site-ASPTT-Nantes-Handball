{% extends 'AppBundle:Admin:layout.html.twig' %}

{% block body %}
    <h1>Edition Charte</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Edit" />
    {{ form_end(edit_form) }}

    <ul>
        <li>
            <a href="{{ path('admin_fairplay_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
