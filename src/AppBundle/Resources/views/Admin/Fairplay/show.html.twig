{% extends 'AppBundle:Admin:layout.html.twig' %}

{% block body %}
    <h1>Fairplay</h1>

    <table class="table table-bordered">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ fairPlay.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ fairPlay.title }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ fairPlay.content }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_fairplay_index') }}"class="btn btn-primary">retour</a>
        </li>
        <li>
            <a href="{{ path('admin_fairplay_edit', { 'id': fairPlay.id }) }}"class="btn btn-warning">modifier</a>
        </li>
    </ul>
{% endblock %}
