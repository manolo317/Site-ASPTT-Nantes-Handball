{% extends 'AppBundle:Admin:layout.html.twig' %}

{% block body %}
    <h1>Guide d'utilisation</h1>

    <div class="panel panel-info">
        <div class="panel-heading">
            <h2>Ajouter une équipe</h2>
            <img class="img-responsive" src="{{ asset('bundles/app/images/guide/navbar_team.png') }}" alt="training" />
            <p>Rendez-vous sur la page de gestion des équipes via l’onglet "Equipes" ci-dessus:</p>
        </div>
        <div class="panel-body">
            <div class="row">
                <img class="img-responsive img-float" id="myImg1" src="{{ asset('bundles/app/images/guide/ajout_team.png') }}" alt="team" />
                <p>Vous accédez à une vue d’ensemble de la liste des équipes.</p>
                <p>Vous accédez aux différentes options de gestion des équipes, pour ajouter un nouvel entrainement, cliquez sur le bouton
                    <a href="{{ path('admin_team_new') }}" class="btn btn-primary">Nouvelle équipe</a></p>
                <p>Vous accédez au formulaire de création d'une'équipe.</p>
                <p>Vous pouvez choisir de charger une photo d’équipe via la fonction d’upload <img id="myImg2" src="{{ asset('bundles/app/images/guide/image.png') }}" alt="image upload"></p>
                <p>Une fois le formulaire rempli, cliquez sur le bouton <button class="btn btn-success">ajouter</button></p>
            </div>
            <!-- The Modal -->
            <div id="myModal" class="modal">

                <!-- The Close Button -->
                <span class="close" onclick="document.getElementById('myModal').style.display='none'">&times;</span>

                <!-- Modal Content (The Image) -->
                <img class="modal-content" id="img01">

                <!-- Modal Caption (Image Text) -->
                <div id="caption"></div>
            </div>
        </div>

    </div>
    <div class="container">
        <ul class="pagination">
            <li><a href="{{ path('admin_guide_edit_training') }}">&laquo;</a></li>
            <li><a href="{{ path('admin_guide_index') }}">menu</a></li>
            <li><a href="{{ path('admin_guide_edit_team') }}">&raquo;</a></li>
        </ul>
    </div>
{% endblock %}